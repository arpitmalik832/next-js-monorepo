/**
 * Contains the knip configuration for the library.
 * @file This file is saved as `knip.config.js`.
 */
const config = {
  entry: ['src/app/layout.jsx'],
  project: ['{src,public}/**/*.{mjs,cjs,js,ts,jsx,css,scss,json,md,mdx}'],
};

export default config;
